@model List<InnoPV.Web.Models.Admin.StudyMasterViewModel>

@{
    ViewData["Title"] = "Study Master";
}

<div class="container-fluid mt-3">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h3>Study Master</h3>
            <p class="text-muted mb-0">
                Manage study, protocol and sponsor mapping details.
            </p>
        </div>

        <div>
            <a asp-controller="AdminMaster"
               asp-action="Index"
               class="btn btn-outline-secondary">
                Back
            </a>

            <a asp-controller="AdminMaster"
               asp-action="CreateStudy"
               class="btn btn-primary">
                + Add Study
            </a>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">
            @TempData["SuccessMessage"]
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            @TempData["ErrorMessage"]
        </div>
    }

    <div class="card shadow-sm">
        <div class="card-header bg-light">
            <strong>Study List</strong>
        </div>

        <div class="card-body table-responsive">
            <table class="table table-bordered table-striped align-middle">
                <thead class="table-light">
                    <tr>
                        <th style="width:60px;">#</th>
                        <th>Study Title</th>
                        <th>Protocol No.</th>
                        <th>Study Code</th>
                        <th>Sponsor</th>
                        <th>Indication</th>
                        <th>Study Type</th>
                        <th style="width:100px;">Status</th>
                        <th style="width:190px;">Action</th>
                    </tr>
                </thead>

                <tbody>
                    @if (Model.Any())
                    {
                        var index = 1;

                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@index</td>
                                <td>@item.StudyTitle</td>
                                <td>@item.ProtocolNo</td>
                                <td>@item.StudyCode</td>
                                <td>@item.SponsorName</td>
                                <td>@item.Indication</td>
                                <td>@item.StudyType</td>
                                <td>
                                    @if (item.IsActive)
                                    {
                                        <span class="badge bg-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Inactive</span>
                                    }
                                </td>
                                <td>
                                    <a asp-controller="AdminMaster"
                                       asp-action="EditStudy"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-outline-primary">
                                        Edit
                                    </a>

                                    <form asp-controller="AdminMaster"
                                          asp-action="ToggleStudyStatus"
                                          method="post"
                                          class="d-inline"
                                          onsubmit="return confirm('Are you sure you want to change this study status?');">
                                        @Html.AntiForgeryToken()

                                        <input type="hidden" name="id" value="@item.Id" />

                                        <button type="submit" class="btn btn-sm btn-outline-warning">
                                            @(item.IsActive ? "Deactivate" : "Activate")
                                        </button>
                                    </form>
                                </td>
                            </tr>

                            index++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="9" class="text-center text-muted">
                                No study records found.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>