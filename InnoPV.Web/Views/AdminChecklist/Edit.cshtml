@model InnoPV.Web.Models.Admin.ChecklistMasterViewModel

@{
    ViewData["Title"] = "Edit Checklist";
}

<div class="container-fluid mt-3">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h3>Edit Checklist</h3>
            <p class="text-muted mb-0">
                Update role-wise checklist details.
            </p>
        </div>

        <a asp-controller="AdminChecklist"
           asp-action="Index"
           class="btn btn-outline-secondary">
            Back
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-light">
            <strong>Checklist Details</strong>
        </div>

        <div class="card-body">
            <form asp-controller="AdminChecklist"
                  asp-action="Edit"
                  method="post"
                  autocomplete="off">

                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Id" />

                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                @await Html.PartialAsync("_ChecklistForm", Model)

                <button type="submit" class="btn btn-primary">
                    Update
                </button>

                <a asp-controller="AdminChecklist"
                   asp-action="Index"
                   class="btn btn-outline-secondary">
                    Cancel
                </a>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}