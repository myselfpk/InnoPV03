@model InnoPV.Web.Models.Admin.ChecklistItemViewModel

@{
    ViewData["Title"] = "Edit Checklist Item";
}

<div class="container-fluid mt-3">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h3>Edit Checklist Item</h3>
            <p class="text-muted mb-0">
                Checklist: <strong>@Model.ChecklistName</strong>
            </p>
        </div>

        <a asp-controller="AdminChecklist"
           asp-action="Items"
           asp-route-checklistMasterId="@Model.ChecklistMasterId"
           class="btn btn-outline-secondary">
            Back
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-light">
            <strong>Checklist Item Details</strong>
        </div>

        <div class="card-body">
            <form asp-controller="AdminChecklist"
                  asp-action="EditItem"
                  method="post"
                  autocomplete="off">

                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="ChecklistMasterId" />
                <input type="hidden" asp-for="ChecklistName" />

                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                @await Html.PartialAsync("_ChecklistItemForm", Model)

                <button type="submit" class="btn btn-primary">
                    Update
                </button>

                <a asp-controller="AdminChecklist"
                   asp-action="Items"
                   asp-route-checklistMasterId="@Model.ChecklistMasterId"
                   class="btn btn-outline-secondary">
                    Cancel
                </a>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}